# Never commit credentials or local configuration.
credentials
*.env
*.conf
config/

# Runtime data and logs.
*.log
*.pid
*.lock
autoauth.log*

# Windows credentials are DPAPI-encrypted but still never belong in Git.
credential.xml
credential.*.tmp

# Editor and OS files.
.DS_Store
*.swp
*~
.idea/
.vscode/
